---
const { info } = Astro.props
---
<div>
    <a href={ info.url }>

        <div class="col-span-1 bg-z-800 rounded-md shadow-xl p-3">
            <div class="min-h-54 md:h-40 rounded-md mb-3">
                <img src={ info.frontmatter.img } alt="" class="w-full h-full object-cover">
            </div>
            <h3 class="">{ info.frontmatter.name }</h3>
            <h3 class="font-extrabold tracking-wider text-lg">{ info.frontmatter.price }</h3>
        </div>

    </a>

</div>