<div  class="border-2 border-blue-700  rounded-md flex items-center gap-2 mb-10 w-4/5 mx-auto overflow-hidden">
    <input placeholder="Search product name" type="text" name="" id="search" class="md:text-base text-sm search bg-blue-50 md:p-2 px-2 w-full outline-none">
    <button type="submit" class="md:py-2 md:px-4 p-1 bg-blue-700 text-white flex items-center h-full">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="md:w-6 md:h-6 md:mx-4 w-4 h-4">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z" />
            </svg>
    </button>
</div>

<script is:inline>
    
    const form = document.querySelector("#search")
    form.addEventListener("input", (e) => {
        e.preventDefault()
        const blogPosts = [...document.querySelectorAll('.blog-post')]; 
        const query = document.querySelector("#search").value.toLocaleLowerCase()
        blogPosts.forEach(post => {
            const title = post.querySelector('.post-title').textContent.toLowerCase();
            const tag = post.querySelector('.post-tag').textContent.toLowerCase();
            const content = post.querySelector('.post-content').textContent.toLowerCase();
            if (title.includes(query) || content.includes(query) || tag.includes(query)) {
                post.style.display = 'block';
                console.log("trying to remove")
            } else {
                post.style.display = 'none';
            }
        })
    })
</script>